# Frontend + Backend microservices (with Docker Compose) example

#### Docker Compose

```shell
docker compose build
docker compose up
```

#### Kubernetes Cluster

```shell
kubectl apply -f ./k8s/
```
